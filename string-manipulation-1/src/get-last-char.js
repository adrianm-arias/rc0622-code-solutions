/* exported getLastChar */
// find index number of first character
// extract first character of string
// return first character of string

function getLastChar(string) {
  var last = string.length - 1;
  var first = string.charAt(last);
  return first;
}

// find index number of last character using .length - 1
// extract last character of string with charat
// return last character of string
