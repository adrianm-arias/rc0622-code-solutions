/* exported getValue */
// search through object properties
// return key of indicated property

function getValue(object, key) {
  return object[key];
}

// search through object properties using for in loop
// returned key of indicated value
